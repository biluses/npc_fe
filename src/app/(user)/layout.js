import React from "react";

const Userlayout = ({ children }) => {
    return (
        <div className="max-w-[393pt] h-full max-h-[852pt] m-auto w-full bg-white ">
            {children}
        </div>
    );
};

export default Userlayout;
