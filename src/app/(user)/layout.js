import React from "react";

const Userlayout = ({ children }) => {
    return (
        <div className="max-w-[393pt] h-full max-h-[852pt] m-auto w-full p-4 bg-white ">
            {children}
        </div>
    );
};

export default Userlayout;
